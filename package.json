{
  "name": "hirecar-marketwatch",
  "version": "1.0.0",
  "description": "HIRECAR MarketWatch – News & Media site",
  "private": true,
  "scripts": {
    "lint": "echo 'No lint step configured' && exit 0",
    "type-check": "echo 'No type-check step configured' && exit 0",
    "test": "echo 'No tests configured' && exit 0",
    "build": "echo 'Static site – no build step required' && exit 0"
  },
  "engines": {
    "node": ">=20"
  }
}
